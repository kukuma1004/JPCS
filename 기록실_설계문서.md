# 기록실 — 학생 기록 · 학습일지 · 상담 관리 시스템

Firebase 프로젝트 `jpsc-99ba3` 기준 · 2026-08-04

---

## 0. 먼저 할 일 (Firebase 콘솔 5단계)

지금 주신 설정은 **Realtime Database** 프로젝트입니다. 이 앱은 Firestore를 쓰므로 아래 순서대로 준비해주세요. 10분이면 끝납니다.

| # | 위치 | 할 일 |
|---|---|---|
| 1 | Firestore Database | **데이터베이스 만들기** → **Standard 버전** → ID `(default)` → 위치 `asia-northeast3 (서울)` → 프로덕션 모드 |
| 2 | Firestore → 규칙 | `firestore.rules` 내용을 붙여넣고 **게시** 버튼 클릭 |
| 3 | Authentication → 로그인 방법 | **이메일/비밀번호** 사용 설정 |
| 4 | 프로젝트 설정 → 내 앱 | 웹 앱(`</>`)이 없으면 하나 등록하고 `firebaseConfig` 복사 |
| 5 | 앱 로그인 화면 | 프로젝트 이름 확인 → 다르면 **변경** → firebaseConfig 붙여넣기 |
| 6 | Authentication → 설정 → 승인된 도메인 | 배포 주소만 남기고 정리 |
| 7 | Hosting | `index.html` + `skills.js` 배포 |

**1단계 주의** — 버전은 **Standard**를 고르세요 (Enterprise는 MongoDB 호환용이라 불필요하고 비쌉니다). 데이터베이스 ID는 반드시 `(default)` 그대로 두세요. 다른 이름으로 만들었다면 앱의 Firebase 연결 설정에서 그 이름을 적어주셔야 합니다. 위치는 나중에 못 바꿉니다.

```bash
npm install -g firebase-tools
firebase login
firebase init hosting        # public 폴더에 index.html, skills.js 넣기
firebase deploy
```

배포 전이라도 `index.html`을 더블클릭하면 **데모 모드**로 전체 기능을 둘러볼 수 있습니다. 다만 로그인·공유 링크는 실제 배포 주소에서만 동작합니다(Firebase Auth는 `file://`를 허용하지 않습니다).

**첫 로그인 순서**: 배포 주소 접속 → `학교 처음 등록하기` → 학교명·성함·이메일·비밀번호 입력 → 관리자 계정 생성 → 설정 화면에서 학생 등록 → 접속코드 인쇄해서 배부.

---

## 0-1. "네트워크 연결을 확인해주세요" 가 뜰 때

**파일을 더블클릭해서 여셨다면 그게 원인입니다.** 주소가 `file://`로 시작하면 Firebase 로그인이 아예 동작하지 않습니다. 브라우저 보안 정책상 로그인은 `http`/`https` 주소에서만 됩니다. 이 경우 앱이 화면 위쪽에 안내를 띄웁니다.

해결은 둘 중 하나입니다.

**① 배포하기 (권장)** — 어차피 학생들이 써야 하니 결국 해야 하는 일입니다.

```bash
firebase deploy
```

**② 내 컴퓨터에서 잠깐 띄우기** — `index.html`과 `skills.js`가 있는 폴더에서 아래 중 하나를 실행하고 `http://localhost:8000` 으로 접속합니다.

```bash
python -m http.server 8000      # 파이썬이 있으면
npx serve -l 8000               # node가 있으면
```

`localhost`는 Firebase **승인된 도메인**에 기본으로 들어 있어 따로 설정할 게 없습니다.

**file:// 이 아닌데도 같은 오류가 난다면** 앱의 연결 진단에 `Firebase 인증 서버 연결` 항목이 추가돼 있습니다. 여기서 ✗가 나면 원인은 보통 이 둘입니다.

- **학교 방화벽·광고 차단 확장이 `googleapis.com`을 막는 경우** — 학교망에서 흔합니다. 휴대폰 테더링으로 한 번 접속해보면 바로 갈립니다
- **이메일/비밀번호 로그인이 꺼져 있는 경우** — 콘솔 → Authentication → Sign-in method → 이메일/비밀번호 **사용 설정**

---

## 0-2. "Missing or insufficient permissions" 가 뜰 때

Firestore가 요청을 거부했다는 뜻입니다. 앱 로그인 화면의 **연결 진단** 버튼을 누르면 어느 단계에서 막히는지 하나씩 짚어줍니다.

```
✓ Firebase SDK 연결
✗ Firestore 데이터베이스 · 보안 규칙 배포   ← 여기서 막히면 아래 ①②
✓ 로그인 상태
✗ 내 프로필 읽기 (users/내 uid)            ← 여기서만 막히면 아래 ③
✓ students 목록 조회
...
```

원인은 거의 항상 넷 중 하나입니다.

**⓪ 다른 프로젝트를 보고 있음 — 새 프로젝트를 만들었다면 여기부터**
앱 코드에 들어있는 설정은 처음 주신 `jpsc-99ba3` 프로젝트를 가리킵니다. Firebase 콘솔에서 **다른 프로젝트를 새로 만들었다면 앱은 여전히 옛 프로젝트를 보고 있어서**, 콘솔에서 아무리 Firestore를 만들고 규칙을 게시해도 바뀌지 않습니다.

로그인 화면 위쪽에 지금 연결된 프로젝트 이름이 표시됩니다. 콘솔에서 작업 중인 프로젝트와 다르면 옆의 **변경** 버튼을 누르고:

1. Firebase 콘솔 → **프로젝트 설정**(⚙ 톱니바퀴) → 아래로 스크롤 → **내 앱**
2. 웹 앱이 없으면 `</>` 아이콘을 눌러 하나 만듭니다 (앱 닉네임만 넣고 등록, Hosting 체크는 선택)
3. **SDK 설정 및 구성** → **구성** 선택 → `firebaseConfig` 블록 전체 복사
4. 앱의 붙여넣기 칸에 그대로 붙여넣고 저장

`import` 줄이나 주석이 섞여 있어도 알아서 값만 뽑아냅니다. 저장하면 앱이 자동으로 다시 시작합니다.

이 방식이라 앞으로는 파일을 고칠 필요가 없고, 다른 학교에 배포할 때도 각 학교 선생님이 자기 프로젝트 설정만 넣으면 됩니다.


**① Firestore가 아직 없거나 Datastore 모드로 만들어짐**
콘솔 → Firestore Database → 데이터베이스 만들기 → **Native 모드** → 위치 `asia-northeast3`.
받으신 설정에 `databaseURL`이 있는 걸 보면 지금은 Realtime Database만 있는 상태입니다. 둘은 이름만 비슷하고 완전히 다른 제품이라, Firestore를 따로 만들어야 합니다.

**② 보안 규칙이 게시되지 않음**
프로덕션 모드로 만들면 기본 규칙이 `allow read, write: if false;` 입니다. 전부 막힙니다.
Firestore → **규칙** 탭 → `firestore.rules` 내용 전체를 붙여넣기 → **게시** 버튼. 붙여넣기만 하고 게시를 안 누르면 적용되지 않습니다.

**③ 규칙 배포 전에 만든 계정이 남아있음**
계정(Auth)은 만들어졌는데 프로필 문서(Firestore)는 규칙에 막혀 못 만들어진 상태입니다. 로그인은 되지만 그 뒤가 전부 막힙니다.
→ 진단 화면 또는 오류 메시지 아래의 **"로그아웃하고 처음부터"** 를 누르고 다시 학교를 등록하세요. 같은 이메일을 다시 써도 됩니다 (앱이 알아서 기존 계정으로 로그인한 뒤 프로필만 새로 만듭니다).

### 그래도 안 되면 — 원인 가르기

`firestore.rules.임시-설치용.txt` 를 규칙 탭에 붙여넣고 게시해보세요. 로그인한 사람이면 누구나 읽고 쓸 수 있는 느슨한 규칙입니다.

- **이걸로 되면** → 규칙 문제입니다. 바로 정식 `firestore.rules`로 되돌리고, 앱의 연결 진단에서 어느 컬렉션이 막히는지 확인해 알려주세요.
- **이걸로도 안 되면** → Firestore 자체 문제입니다 (①번).

⚠️ 임시 규칙은 확인용입니다. **실제 학생 데이터를 넣기 전에 반드시 정식 규칙으로 되돌리세요.**

---

## 1. 왜 웹앱(PWA)인가

정적 웹사이트로는 불가능합니다. 로그인, 교사/학생 권한 분리, 데이터 영구 저장, 통계 집계가 전부 필요합니다. 네이티브 앱도 아닌 이유는 **설치율이 곧 사용률의 상한**이 되기 때문입니다. 학생 35명 중 5명이 "앱 설치가 안 돼요"라고 하는 순간 데이터에 구멍이 나고, 구멍 난 데이터는 교사가 신뢰하지 않게 되며, 신뢰하지 않는 도구는 곧 안 쓰게 됩니다.

빌드 도구(React/Vite/webpack)를 일부러 쓰지 않았습니다. 이 시스템을 1년 뒤 유지보수할 사람이 선생님 본인일 가능성이 높은데, `npm install`이 깨져서 손을 못 대는 상황이 학교 자체 제작 도구가 죽는 가장 흔한 이유입니다. 파일 두 개면 메모장으로도 고칠 수 있습니다.

---

## 2. 권한 3단계와 로그인

### 접속 화면부터 갈라집니다

첫 화면에서 **학생 / 선생님** 두 갈래로 나뉩니다. 학생 쪽은 코드 입력창 하나뿐이고 이메일·비밀번호 같은 건 아예 보이지 않습니다.

한 번 고르면 그 선택을 기억해서, 다음부터는 바로 해당 화면으로 들어갑니다. 학생에게는 아래 링크를 주면 갈림길 없이 곧장 코드 화면이 뜹니다.

```
https://내주소/?student      ← 학생용
https://내주소/?teacher      ← 교사용
```

### 권한 3단계

| 권한 | 되는 일 | 되는 방법 |
|---|---|---|
| **호스트** (운영자) | 여러 학교 개설·목록·가입코드 관리 | Firebase 콘솔에서 `hosts/{내uid}` 문서 직접 생성 |
| **학교 관리자** | 그 학교의 교사 배정, 학년도, 학생 명단, 가입코드 | 학교를 개설하거나 **관리자 가입코드**로 가입 |
| **교사** | 배정된 학급의 학생만 | **교사 가입코드**로 가입 |

가입코드가 두 종류가 됐습니다. 학교를 만들면 `교사 가입코드`와 `관리자 가입코드`가 함께 발급되고, 어느 코드로 가입하느냐에 따라 권한이 정해집니다. 관리자 코드는 꼭 필요한 분에게만 주시면 됩니다.

### 호스트 등록 방법

호스트는 **앱에서 스스로 될 수 없습니다.** 콘솔에서 직접 문서를 만들어야만 합니다.

1. 먼저 평범하게 교사 계정으로 로그인해서 본인 uid를 확인합니다 (연결 진단 화면에 `uid abc12345…` 로 표시)
2. Firebase 콘솔 → Firestore Database → **컬렉션 시작** → 컬렉션 ID `hosts`
3. 문서 ID에 그 uid를 넣고, 필드는 아무거나 하나 (`name: "운영자"`) 넣고 저장
4. 앱에서 로그아웃 후 다시 로그인하면 **호스트 콘솔**이 열립니다

### 호스트는 학생 정보를 볼 수 없습니다

의도한 설계입니다. 호스트 콘솔에는 학교 이름·개설일·학년도·가입코드·집계 수치만 나옵니다. **학생 이름, 생기부 기록, 학습일지, 상담 내용은 보안 규칙에서 막혀 있어 어떤 경로로도 접근할 수 없습니다.**

여러 학교를 한 프로젝트에 모으면 운영자가 사실상 전국 학생 개인정보의 처리자가 된다는 게 앞서 짚었던 문제인데, 호스트 권한을 "학교와 계정 관리"로만 한정하면 그 위험이 사라집니다. 학생 데이터는 각 학교 관리자와 담당 교사만 봅니다.

집계 수치(학생 수, 기록 수)는 각 학교 관리자가 로그인할 때 학교 문서에 요약만 남기는 방식이라, 이 역시 개인정보를 담지 않습니다.

---

## 2-1. 로그인 방식

| 대상 | 방식 | 비고 |
|---|---|---|
| **관리자** | 이메일 + 비밀번호 | 학교를 처음 등록한 선생님. 학교 ID와 교사 가입코드를 발급받음 |
| **교사** | 이메일 + 비밀번호 | 가입 시 학교 ID + 교사 가입코드 필요 |
| **학생** | 6자리 접속코드 | 이메일·비밀번호 없음. 교사가 발급, 계속 유지 |

학생 접속코드는 `A1B2C3` 형태 6자리입니다. 혼동하기 쉬운 `0/O/1/I`를 뺀 32자 알파벳을 써서 조합이 약 10억 가지입니다. 설정 화면의 **접속코드 인쇄**로 명함 크기 배부표를 뽑아 나눠주시면 됩니다.

내부적으로는 코드를 Firebase Auth 계정으로 변환해 로그인하므로, 학생이 개인정보를 추가로 제출할 필요가 없습니다. 코드를 분실하면 교사가 **코드 재발급**을 누르면 되고, 이전 코드는 즉시 무효가 됩니다.

---

## 3. 화면 구성

### 교사

| 화면 | 하는 일 |
|---|---|
| **대시보드** | 오늘 미작성 학생, 반 평균 공부시간, 기록 진행률, **공부시간이 눈에 띄게 줄어든 학생 알림** |
| **학생** | 학생 × 5개 영역 매트릭스. 상담 횟수·최근 공부량 함께 표시 |
| **학생 상세** | 학습일지 / 상담 / 리포트 / 세특·동아리·자율·진로·행특 |
| **학습일지 분석** | 공부시간 추이, 과목별 배분, 어려워하는 내용 과목별 모음, 세특 소재 후보, 기록 성실도 |
| **내보내기** | Claude 프롬프트 생성 또는 앱 안에서 일괄 생성 |
| **설정** | API 키, 모델, 학생 등록·접속코드 관리, 학교 정보 |

대시보드의 **공부시간 급감 알림**이 상담 도구로서 이 시스템의 핵심입니다. 지난주 대비 35% 이상 줄어든 학생을 자동으로 띄웁니다. 학습량 급감은 정서적 어려움·가정 문제·교우관계 변화의 가장 이른 신호인 경우가 많은데, 40명을 눈으로 훑어서는 잡히지 않습니다.

### 학생 (모바일 우선)

**오늘 일지 쓰기** — 과목+시간 / 어려웠던 내용 / 한 줄 소감 / 더 알아보고 싶어진 것. 이게 전부입니다. 3분 안에 끝나야 매일 씁니다.
**내 기록** — 2주 공부시간 그래프, 과목별 배분, 지난 일지, 연속 기록일.

---

## 4. 상담 기능 4종

### ① 학습 리포트 (인쇄 · PDF)

기간(14/28/56/90일)을 골라 A4 한 장으로 출력합니다. 담긴 내용은 총 공부시간, 기록 성실도, 공부시간 추이 그래프, 과목별 배분, **본인이 적은 어려웠던 내용**, **본인이 적은 궁금증**, **앞으로의 학습 방향**, 담임 코멘트입니다.

**앞으로의 학습 방향과 담임 코멘트는 `✦ 코멘트·학습방향 생성` 버튼으로 초안이 만들어집니다.** 프롬프트에 못 박아둔 조건:

- 일반론 금지 — "복습을 꾸준히 하세요"가 아니라 그 학생의 기록에서 출발해야 합니다. 어려워한 내용이 조건부확률에 몰려 있으면 그 지점을 짚고, 특정 과목 공부시간이 0에 가까우면 그 사실에서 시작합니다
- 각 항목은 [무엇을] + [어떻게] + [왜 도움이 되는지] 순서로, 오늘 당장 시작할 수 있을 만큼 구체적으로
- 학생이 스스로 궁금해한 것이 있으면 탐구로 이어가는 제안을 최소 1개
- 이미 공부시간이 많은 학생에게 더 늘리라고 하지 않음 (지킬 수 없는 양 금지)
- 사교육·문제집 구매 권유 금지, 다른 학생과 비교 금지, 성격 판정 금지
- 부모가 아이를 다그치게 만드는 문장 금지 — 부족한 점은 반드시 개선 방법과 붙여서

**생성된 글은 반드시 읽고 고친 뒤 내보내세요.** 학부모가 읽는 글이라 잘못된 사실 하나가 신뢰를 크게 깎습니다.

**성적·석차는 일부러 넣지 않았습니다.** 그건 이미 다른 문서에 있고, 리포트에 넣는 순간 학부모의 시선이 거기로 쏠려 나머지가 안 보입니다. 이 리포트의 값어치는 "우리 아이가 뭘 어려워하는지, 뭘 궁금해하는지"를 부모가 처음 보게 되는 데 있습니다.

**생기부 초안 문안은 리포트에 포함되지 않습니다.** 활동 기록의 원본 입력 일부만 요약으로 들어갑니다.

### ② 상담 기록 관리

날짜 / 형태(학생 대면·학부모 전화·학부모 대면·수시 면담) / 상담 내용 / **약속한 것** / **다음에 확인할 것**.

뒤의 두 항목이 핵심입니다. 상담이 효과를 잃는 가장 흔한 이유가 "지난번에 뭘 약속했는지 서로 기억을 못 해서"인데, 다음 상담 화면에 지난 약속이 그대로 떠 있으면 대화가 "잘 지냈니"가 아니라 "지난번에 정한 그거 어떻게 됐어"로 시작됩니다.

### ③ 상담 전 브리핑 (AI)

학습일지·활동 기록·지난 상담을 종합해 5개 항목으로 정리합니다: 한눈에 / 눈에 띄는 변화 / 강점 / **물어볼 것** / 학부모께 전할 만한 것.

프롬프트에 못을 박아둔 원칙:
- 데이터에 있는 사실만. 없으면 "판단할 데이터 부족"이라고 솔직히 적음
- 성격 판정("게으르다", "의지가 약하다") 금지 — 관찰된 행동 패턴과 변화만
- 수치는 반드시 근거와 함께 ("공부시간이 줄었다" ❌ → "일평균 3시간 20분 → 1시간 50분" ✅)
- 학습 데이터만으로 정서·가정 문제를 단정하지 않음

### ④ 학부모 열람 링크

리포트를 만든 시점 그대로 **스냅샷으로 굳혀서** 저장하고 링크를 발급합니다. 30일 후 만료됩니다.

살아있는 데이터베이스에 접근 권한을 주는 방식이 아니라는 점이 중요합니다. 링크를 아는 사람은 그 리포트 한 장만 볼 수 있고, 다른 학생 정보나 생기부 기록에는 어떤 경로로도 닿을 수 없습니다. 이후 데이터가 바뀌어도 링크 내용은 갱신되지 않는데, 이건 제약이 아니라 의도한 동작입니다 — 학부모께 보낸 자료가 나중에 몰래 바뀌지 않아야 합니다.

---

## 4-0. 교사별 담당 학급과 학년도

### 담당 학급

| 권한 | 볼 수 있는 범위 |
|---|---|
| **관리자** (학교를 처음 등록한 교사) | 학교 전체 |
| **교사** | 배정된 학급의 학생만 |

관리자가 **설정 → 교사 관리**에서 각 교사의 담당 학급을 `3-2, 3-5` 형태로 배정합니다. 배정 전에는 학생 목록이 비어 있고 안내 문구가 뜹니다.

**담임 학급**을 따로 지정할 수 있습니다. 담임이 아닌 교사가 행동특성 및 종합의견 탭을 열면 "담임이 작성하는 항목입니다" 표시가 붙습니다. 막지는 않습니다 — 공동담임이나 기간제 교사가 대신 쓰는 경우가 있어서입니다.

**규칙 수준이 아니라 화면 수준의 분리입니다.** 문서마다 담당 학급을 대조하려면 읽기 비용이 기하급수적으로 늘어 실용적이지 않습니다. 같은 학교 교직원은 NEIS에서도 서로의 담당 학생 정보에 접근 가능한 위치이므로, 이 시스템은 **학교 단위 격리(규칙) + 담당 단위 분리(화면)** 를 기준으로 삼았습니다. 학교 밖으로는 어떤 경로로도 나가지 않습니다.

### 학년도와 데이터 연속성

학생을 **사람 단위로 저장**하고 학년·반·번호는 학년도별 정보로 붙입니다. 학번은 해마다 바뀌지만 **접속코드는 3년 내내 같습니다.** 학생은 1학년 때 받은 코드로 3학년까지 그대로 로그인합니다.

```
students/{id}
  name, accessCode(3년 유지), cohort(입학년도)
  year, grade, klass, number, code, classKey    ← 현재 학년도
  history: [{year:2025, grade:2, klass:5, number:9}, ...]   ← 지난 학년도
```

`records`에는 `year`가 붙고, `journals`는 날짜로 학년도를 판별합니다(3월 1일 ~ 다음해 2월 말). 왼쪽 사이드바의 학년도 선택으로 지난해 기록을 그대로 볼 수 있고, 학생 상세 화면 위에는 학년도 이력이 `2026 · 3-2-3 | 2025 · 2-5-2 | 2024 · 1-3-5` 형태로 표시됩니다.

**3월에 할 일** — 설정에서 현재 학년도를 올리고, 새 명단을 엑셀로 올립니다.

### 진급 처리

핵심은 **접속코드 열**입니다.

1. 지난 학년도를 고른 뒤 **"지금 데이터 내보내기"** → 접속코드가 채워진 파일을 받습니다
2. 학생명단 시트에서 **학년도·학년·반·번호만** 새로 고칩니다 (접속코드는 그대로 둡니다)
3. 업로드하면 같은 학생으로 이어집니다

접속코드가 있으면 기존 학생의 새 학년도 정보로 갱신되고 지난 학년도는 이력으로 내려갑니다. 접속코드를 비우면 새 학생으로 등록되고 코드가 새로 발급됩니다 — 전학생은 그렇게 넣으시면 됩니다.

업로드 전에 `학년도 갱신 3명 — 강도윤 → 2027 3-7-5` 처럼 미리보기가 나오니 확인하고 저장하시면 됩니다.

---

## 4-1. 학생 등록과 엑셀 일괄 입력

### 학생은 교사가 등록합니다

학생이 스스로 가입하는 구조가 아닙니다. **설정 → 학생 관리**에서 등록하면 6자리 접속코드가 자동 발급되고, 그 코드를 받은 학생만 들어올 수 있습니다. 등록 방법은 세 가지입니다.

| 방법 | 쓰는 상황 |
|---|---|
| 한 명씩 추가 | 전학생 등 한두 명 |
| 여러 명 한번에 | `1 강도윤` 형태로 한 줄에 한 명씩 붙여넣기 |
| 엑셀 업로드 | 명단 + 기록을 한꺼번에 |

접속코드는 한 번 발급되면 계속 유지되고, 분실하면 **코드 재발급**으로 새로 줄 수 있습니다(이전 코드는 즉시 무효). **접속코드 인쇄**를 누르면 명함 크기 배부표가 나옵니다.

**학년이 바뀌면** 새 학교를 만들 필요 없이 학생을 새로 등록하시면 됩니다. 이전 학년 데이터는 그대로 남아 있어 3년치 기록이 쌓입니다.

### 엑셀로 한번에 입력

**설정 → 엑셀로 한번에 입력**에 세 개의 버튼이 있습니다.

| 버튼 | 하는 일 |
|---|---|
| **빈 양식 내려받기** | 사용법 + 학생명단 + 세특·동아리·자율·진로·행특 시트가 든 xlsx |
| **지금 데이터 내보내기** | 현재 입력된 내용을 그대로 담아 받기 (엑셀에서 고쳐 다시 올리기용) |
| **엑셀 업로드** | 읽은 내용을 먼저 보여주고, 확인 후 저장 |

각 시트의 열은 앱의 입력 칸과 1:1로 같습니다. 예를 들어 세특 시트는 `학번 | 이름 | 수준 | 목표byte | 과목 | 질문과 그 이유 | 조사하며 알게 된 것 | 다른 관점·사례 | 탐구 후 달라진 생각 | 배운 점·확장` 입니다.

동작 규칙:

- **학번(3-2-14)으로 학생을 찾고, 비어 있으면 이름으로** 찾습니다
- 학생명단 시트에 있는 새 학생은 자동 등록되고 접속코드가 발급됩니다. 같은 파일의 기록 시트에서 그 학생을 바로 참조해도 됩니다
- **빈 칸은 기존 내용을 지우지 않습니다.** 채운 칸만 덮어씁니다. 한 영역씩 나눠서 여러 번 올려도 안전합니다
- 수준을 비우면 A, 목표byte를 비우면 기본값(세특·동아리·자율·진로 1500, 행특 900)
- 학생을 못 찾은 줄은 저장하지 않고 목록으로 알려줍니다
- **시트 이름과 첫 줄 제목은 바꾸지 마세요.** 그 두 가지로 데이터를 찾습니다

작업 흐름은 이렇게 됩니다: 학생들에게 탐구활동지를 구글폼으로 받기 → 응답 시트를 양식에 맞게 열 정리 → 업로드 → `✦ 앱에서 한꺼번에 생성` → 학생별로 문안 검토.

---

## 5. 세특 생성 — 앱 안에서 바로

설정 화면에 Claude API 키를 넣으면 각 기록 화면에 **✦ 특기사항 생성** 버튼이 생깁니다. 내보내기 화면의 **✦ 앱에서 한꺼번에 생성**은 선택한 학생 전원을 순차 처리하고 결과를 실시간으로 보여줍니다.

### 품질 규칙은 `skills.js`에 있습니다

세특 스킬 5종의 규칙을 시스템 프롬프트로 압축해 넣었습니다. 영역별 5,000자 안팎입니다.

| 항목 | 내용 |
|---|---|
| 공통 | 7문장 고정, 문장별 byte 배분표, 수준별 문체 3단계, 기재 금지 11항목, 절대 규칙 20여 개 |
| 세특 | 과목별 사고 흐름 10종, 체육·예술 필수 기재 요소(실기능력·교과적성) |
| 동아리 | 참여도·협력도·열성도·활동실적 근거, 자율동아리 30자 소개 규정 |
| 자율 | 질문 생성 → 비교(기준 명시) → 판단 변화 → 질문 재구성, 비교 최소 1회 |
| 진로 | 관심 → 판단 → 비교 → 변화 → 교과 연결 → 확장 |
| 행특 | 900byte, 교과 용어의 행동·태도 추상화, 여러 관찰자 데이터 통합 |

**학교 지침이 다르면 `skills.js`만 고치면 됩니다.** 앱 코드는 건드릴 필요가 없습니다.

### 비용과 안전

API 키는 **이 브라우저에만** 저장되고 서버로 전송되지 않습니다. 공용 PC에서는 쓰지 마세요.

학생 1명 · 세특 1건당 대략 입력 7,000토큰 / 출력 700토큰입니다. 40명 5개 영역이면 200건이니, 실제 요금은 [Anthropic 요금 페이지](https://www.anthropic.com/pricing)에서 현재 단가를 확인해 곱해보시길 권합니다. 초안은 Haiku, 최종본만 Sonnet/Opus로 돌리는 식으로 모델을 바꿔가며 쓰면 비용이 크게 줄어듭니다.

**AI에 보낼 때 학생 이름 대신 학번 코드를 쓰는 옵션이 기본으로 켜져 있습니다.** 실명을 외부 API에 보내지 않아도 결과 품질에는 차이가 없습니다. 끄지 마시기를 권합니다.

### 생성 후에는 반드시 확인하세요

생성된 문안은 오른쪽 칸에 자동으로 들어가고 바이트·문장 수가 실시간 검사됩니다. AI가 7문장 규칙이나 목표 분량을 어기는 경우가 있으니 숫자를 확인하고, 사실관계는 반드시 교사가 직접 검토해주세요. **AI는 초안 작성 도구이지 최종 결정권자가 아닙니다.**

---

## 6. 데이터 모델

```
users/{uid}
  schoolId, role('admin'|'teacher'|'student'), name, studentId?, approved

codes/{접속코드}              ← 로그인 전 조회용. 담긴 값은 schoolId/studentId 뿐
shares/{토큰}                 ← 학부모 리포트 스냅샷(HTML 통째로) + 만료일

schools/{sid}
  name, teacherCode, ownerUid
  │
  ├── students/{id}     grade, klass, number, name, code, accessCode
  ├── records/{id}      studentId, area, level, target, meta{}, fields{}, text, status
  ├── journals/{id}     studentId, date, totalMinutes, subjects[], difficult[], memo, seed
  ├── counsels/{id}     studentId, date, kind, summary, promise, next
  └── briefs/{studentId} briefing{text,at}, reportNote        ← 교사 전용
```

**`briefs`를 학생 명부와 분리한 이유** — 학생은 자기 명부 문서를 읽을 수 있어야 하는데, 교사의 판단이 담긴 브리핑을 같은 문서에 두면 그게 학생에게 그대로 노출됩니다. 처음 설계에서 이 문제가 있어 별도 컬렉션으로 뺐습니다.

### 학생이 로그인하면 받아오는 데이터

- 본인 명부 문서 1건
- 본인 학습일지 전체

그게 전부입니다. 다른 학생, 생기부 기록, 상담, 브리핑은 **네트워크 요청 단계에서 차단**되며 보안 규칙으로도 이중으로 막혀 있습니다.

**인덱스** — Firestore가 요구하면 콘솔 링크를 눌러 자동 생성하세요. 필요한 것: `journals(studentId, date)`, `records(studentId, area)`.

### 저장 경로는 신경 쓰지 않으셔도 됩니다

Firestore는 SQL 데이터베이스처럼 테이블을 미리 만들어둘 필요가 없습니다. 앱이 저장할 때 위 경로를 알아서 만들고, 컬렉션도 첫 문서가 들어가는 순간 생성됩니다. 콘솔에서 폴더를 파거나 스키마를 정의하는 작업은 없습니다.

교사가 하실 일은 **① Firestore 만들기 ② 규칙 게시** 둘뿐이고, 그 뒤로는 어느 학생의 어느 영역인지 앱이 판단해서 넣습니다. 콘솔의 Firestore 화면에 들어가면 `schools → (학교ID) → records` 식으로 쌓이는 게 보이는데, 확인용으로만 보시고 직접 고치실 필요는 없습니다.

---

## 7. 개인정보 — 운영 전 확인할 것

**1) 최소 수집** — 실명은 `students` 한 곳에만. 주민번호·연락처·주소는 아예 받지 않습니다. NEIS가 이미 하는 일을 중복할 이유가 없습니다.

**2) 익명화** — 외부 AI로 나가는 데이터는 기본적으로 학번 코드만 씁니다.

**3) 학생 열람 차단** — 생기부 초안 문안은 학생이 읽을 수 없습니다. 초안이 노출되면 "이렇게 써주세요" 요구와 민원으로 이어집니다.

**4) 실제 운영 전** — 학생·학부모 개인정보 수집·이용 동의서를 받고, 학교 정보보호 담당 선생님과 사전 협의하시길 권합니다. 학교 밖으로 배포한다면 더욱 그렇습니다.

**5) Firebase API 키에 대해** — 웹 API 키는 원래 클라이언트에 노출되는 값이라 비밀이 아닙니다. 실제 방어선은 `firestore.rules`와 **승인된 도메인** 설정입니다. 이 둘만 제대로 되어 있으면 키가 알려져도 데이터에 접근할 수 없습니다.

---

## 8. 다른 학교에 배포한다면

| | **A. 학교별 Firebase 프로젝트** | **B. 하나의 프로젝트, schoolId 격리** |
|---|---|---|
| 데이터 격리 | 물리적 완전 분리 | 보안 규칙으로 논리 분리 |
| 개인정보 책임 | 각 학교가 각자 | 운영자(선생님)가 전체 |
| 배포 | 학교마다 Firebase 세팅 | URL만 공유 |
| 무료 한도 | 학교마다 별도 | 공유 (학교 늘면 유료 전환) |
| **권장** | ✅ | 같은 재단·교육청 단위일 때만 |

**A안을 권합니다.** 기술 문제가 아니라 책임 소재 문제입니다. B안으로 여러 학교 데이터를 한 프로젝트에 모으면 선생님 개인이 사실상 여러 학교 학생 개인정보의 처리자가 됩니다. 사고가 나면 감당하기 어려운 위치가 됩니다.

배포 방법: `index.html` + `skills.js` + `firestore.rules` + 이 문서를 GitHub에 공개하고, 각 학교 선생님이 자기 Firebase 프로젝트에 배포하도록 안내하시면 됩니다.

---

## 9. 진행 순서 제안

**1주차 — 나 혼자 검증**
설정에서 학생 등록 → 세특 한두 명 끝까지 생성 → 바이트·문장 수 확인 → 폼에 없어서 아쉬운 필드 파악. 여기서 나온 수정사항이 이후 전부에 영향을 줍니다.

**2~3주차 — 학생 투입**
접속코드 배부 → 종례 때 1분씩 같이 쓰는 시간. 첫 2주가 이후 1년을 좌우합니다. 기능이 아니라 습관이 관건입니다.

**4주차 — 상담에 실제로 써보기**
브리핑 생성 → 상담 → 상담 기록 → 리포트 출력. 한 사이클을 돌려보고 리포트에서 빠진 항목을 채웁니다.

**이후 — 학교 확대**
다른 선생님께 학교 ID + 교사 가입코드 전달. 학기 개념(1학기/2학기 기록 분리)과 NEIS 일괄 등재용 엑셀 내보내기는 이 시점에 붙이면 됩니다.

---

## 첨부 파일

| 파일 | 내용 |
|---|---|
| `index.html` | 웹앱 본체 |
| `skills.js` | 생기부 작성 규칙 (여기만 고치면 생성 품질이 바뀜) |
| `firestore.rules` | Firebase 보안 규칙 — **평소에 쓰는 정식본** |
| `firestore.rules.임시-설치용.txt` | 설치 문제 원인 가를 때만 잠깐 쓰는 느슨한 규칙 |

브라우저 콘솔에서 `기록실.reset()` 을 입력하면 데모 데이터가 초기화됩니다.
